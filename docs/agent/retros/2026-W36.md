# Sprint 2.2.0 retro (Week 36: 2026-09-06 〜 2026-09-12)

## TL;DR

11 Issues done (#398, #396, #395, #401, #404, #407, #410, #411, #412, #413, #414, #417) → 12 SP velocity.
WIP=1 厳守. CI 100% green on release-2-2-0 (after #423 trigger expansion). `sync-submodules` workflow
退避 + AGENTS.md dispatcher 化 + responsive audit 35 findings (5 critical → 個別 Issue → 2.2.0 で close).

## Metrics

| 指標 | 値 | 目標 | 評価 |
|---|---|---|---|
| velocity (Done Issues) | 11 + #401 closing | — | 12 SP |
| cycle time (median) | ~15 min | <1 sprint | ✓ very fast (loop engineering) |
| WIP avg | 1.0 | ≤1 | ✓ |
| CI 緑率 (gates ran) | 100% (7/7) | ≥95% | ✓ |
| CI 緑率 (full gate) | 100% (7/7 Bun + Rust) | ≥95% | ✓ #404 fixed |
| PR pile-up 解消 | 253 → 3 | 0 | ✓ |
| Branch 整理 | 264 → 5 | canonical model | ✓ |
| Responsive audit | 35 findings → 5 critical → 4 closed | all critical | ✓ |

## 観点 (5 things)

### 1. なぜ 259 PR が pile-up したか

`chore/sync-submodules-YYYYMMDDTHHMMSSZ` workflow は 15 分 cron で走り、 
`bun.lock` を refresh しないまま PR を作る設計だった (`.github/workflows/sync-submodules.yml` lines 119-125 旧版)。 
merge-safe でない PR が 1 日 20-30 件来ると人間の reviewer が認知破綻 → 全 PR 
0 merge / 0 close の最悪状態。 **feedback loop 欠如**: 「bot が PR を作る → 人間 
が review → merge or close」の連鎖が自動化されておらず、 bot 側に close 機構が無かった。

### 2. なぜ `fix/bun-pm2-deploy-bootstrap` が 1.5ヶ月 放置されたか

WIP=1 violation (該当 branch は main と乖離したまま誰も拾わなかった) + retro スキップ。 
前 sprint (2.1.x) では retro が run されず、 stale branch 検出 mechanism が無かった。 
**学習**: solo agile は retro を **必ず sprint close で** run する discipline が必要。

### 3. sync-submodules をどう再開するか

PR #393 の `agile-premises.md` で "release = `release-x-y-z` → main" を canonical 化したので、 
再開も **直接 main push** 案 (案 A) を推奨:
- submodule drift 検出 → 直接 `git push origin main` で gitlink 更新
- CI は main push で trigger、 PR review は人間の判断に委ねる
- 案 B (weekly cron + PR 1本/week) は次善
- 案 C (submodule 側 pull モデル) は submodule 側にも `PARENT_REPO_TOKEN` が必要で重い

### 4. AGENTS.md dispatcher 化は運用に乗ったか

✓ Yes. `docs/agent/agile-premises.md` が reference され (Issue #398 merge)、 
backlog Issue (#395, #396, #401) や housekeeping 経路で dispatcher が指す docs 群 
(`release-workflow.md` / `quality-security.md` / `parallel-orchestration.md`) を 
実際に読んだ。 dispatcher 化は lightweight で機能している。

### 5. Claude との協働前提 (Plan→承認→実装) は守れたか

✓ Yes, with one deviation:
- Plan (rustling-foraging-treasure.md) は AskUserQuestion 4 つで承認取得後に実行
- housekeeping commit (Step 1) で「main への直接 push は anti-pattern」からの deviation:
  workflow 退避 commit は `chore/disable-sync-submodules-2026-09 → main` の feature branch + PR route にした (CI 安全のため)。 **学習**: housekeep commit も canonical model に乗せるべきだが、 sprint 起算前の commit なので release branch が無い例外として認容。

### 6. Bun 1.3.14 SIGILL 132 flake

`bun run build` が Next.js 16.3.0 の teardown で `bun:sqlite` cleanup 時に SIGILL 132 で落ちる
(run 34018005220 で記録、 34018670564 で再実行で self-healed)。 静的 export (`out/`) は
segfault 前に書き終わっているので、 `set +e` + `out/index.html` 存在確認で tolerate する
workaround を #418 (PR) で実装。 **学習**: teardown 起因の crash は exit code だけ見ても
原因が分からず、 再実行で通る transient も多い。 `out/` artifact の存在で gate する設計は
"書いた成果物で判定" の正しいパターン。

### 7. worker dispatch pattern 観察

loop-engineering で worker agent に Issue body + branch/worktree 制約 + 自己レビュー checklist
を渡すと、 1〜3 分で PR Draft まで完走。 人間が触るのは (1) Issue body の precision (worker の
branch/worktree 命名 + commit message + self-review checklist) と (2) 出力の diff verify のみ。
8 Issue 並列 dispatch (→ 6 PR 並列作成) で supervisor の直列作業時間は合計 ~20 分、 WIP=1
を厳守しつつ velocity 12 SP を達成。 **学習**: worker への input precision が velocity の
bottleneck。 Issue template (acceptance criteria + diff hint + commit message prefix) を
2.3.0 で整備する。

## Improvement proposals (next sprint focus)

1. ~~**CI gap (Issue #404) を必ず 2.3.0 初回で fix**~~ → **DONE in 2.2.0** (PR #423)
2. ~~**retro discipline を ticket化**~~ → **DONE in 2.2.0** (PR #420, auto-retro-pr workflow)
3. **sprint close → main merge の discipline**: solo agile は release branch → main の手動 merge を
   sprint close 時に必ず run する discipline が必要 (Issue #401 のような PR 以外で close しない)。
4. **PR body の auto-close 検出が効かない**: `gh pr merge --squash --body` で渡した本文は
   squash commit message 行き、 PR description は元のまま → "Closes #N" は PR description に
   残っていても GitHub 側で `closingIssuesReferences` を補完しないことがある。 Issue は明示 close 推奨。

## Done in this sprint

- PR #394 chore(ci): disable sync-submodules workflow
- PR #402 docs(agent): ratify agile-premises.md
- PR #403 fix(cms-api): hydrate skips R2 directory-marker keys
- PR #405 feat(cms-api): SKIP_R2=1 local-dev bypass + R2 sync push/pull modes
- PR #418 fix(ci): tolerate Bun 1.3.14 SIGILL 132 in Next.js build (closes #417)
- PR #419 fix(tailwind): add xl/2xl breakpoints (closes #410)
- PR #420 feat(ci): auto-create retro draft PR on sprint close (closes #407)
- PR #421 refactor(tools): use Tailwind responsive layout on /tools (closes #411)
- PR #422 fix(home): remove min-w-[400px] clamp and bump button height (closes #412)
- PR #423 fix(ci): trigger ci.yml on release-* PRs (closes #404)
- PR #424 fix(about/card/real): aspect-ratio responsive layout (closes #414)
- PR #425 fix(workshop): render mobile sidebar in default view (closes #413)

## Deferred to 2.3.0

- #399 chore(cms): migrate 230 legacy CMS DBs to per-SQLite layer (P2/L)
- #400 feat(ci): add Project board auto-status sync workflow (P2/M)
- #415 レスポンシブ audit 残 25 findings (12 major + 12 minor + 1 dup) → 2.3.0 planning で個別 Issue 化
