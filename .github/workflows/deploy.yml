name: Bun Build and Deploy

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.10"

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run lint and format
        run: |
          bun x biome check . --write --unsafe || true
          bun x biome format --write . || true

      - name: Run type check (informational)
        run: bun x tsc --noEmit --skipLibCheck 2>&1 || echo "Type check warnings"

      - name: Run tests
        env:
          NODE_ENV: test
          CI: true
        run: bun run test

      - name: Build project
        env:
          NEXT_PUBLIC_GA_ID: ${{ secrets.NEXT_PUBLIC_GA_ID }}
          NEXT_PUBLIC_SITE_URL: ${{ secrets.NEXT_PUBLIC_SITE_URL }}
        run: bun run build

  deploy:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master'
    needs: test
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.10"

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build project
        env:
          NEXT_PUBLIC_GA_ID: ${{ secrets.NEXT_PUBLIC_GA_ID }}
          NEXT_PUBLIC_SITE_URL: ${{ secrets.NEXT_PUBLIC_SITE_URL }}
          X_BEARER_TOKEN: ${{ secrets.X_BEARER_TOKEN }}
        run: bun run build

      - name: Prepare standalone deployment
        run: |
          cp -r .next/static .next/standalone/.next/static
          [ -d ".next/standalone/public" ] || cp -r public .next/standalone/public
          [ -d ".next/standalone/data" ] || cp -r data .next/standalone/data
          tar -czf deployment-standalone.tar.gz \
            -C .next/standalone \
            server.js \
            .next \
            public \
            data \
            node_modules \
            package.json

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          echo "${{ secrets.GCP_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          cat > ~/.ssh/config << EOF
          Host deploy-server
            HostName ${{ secrets.GCP_HOST }}
            User ${{ secrets.GCP_USER }}
            IdentityFile ~/.ssh/id_rsa
            StrictHostKeyChecking no
          EOF
          chmod 600 ~/.ssh/config

      - name: Transfer deployment package
        run: scp deployment-standalone.tar.gz deploy-server:/home/${{ secrets.GCP_USER }}/deployment-standalone.tar.gz

      - name: Deploy on server
        run: |
          ssh deploy-server 'bash -s' << 'REMOTE'
          set -euo pipefail

          export BUN_INSTALL="$HOME/.bun"
          export PATH="$BUN_INSTALL/bin:$PATH"
          export NVM_DIR="$HOME/.nvm"
          [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"
          [ -n "${NVM_BIN:-}" ] && export PATH="$NVM_BIN:$PATH"

          command -v bun >/dev/null 2>&1 || { echo "bun not found"; exit 1; }
          command -v pm2 >/dev/null 2>&1 || { echo "pm2 not found"; exit 1; }

          APP_DIR="/var/www/yusuke-kim"
          PACKAGE_PATH="$HOME/deployment-standalone.tar.gz"
          TMP_DIR="$(mktemp -d)"

          pm2 delete yusuke-kim 2>/dev/null || true
          mkdir -p "$APP_DIR"
          find "$APP_DIR" -mindepth 1 -maxdepth 1 -exec rm -rf {} +
          tar -xzf "$PACKAGE_PATH" -C "$TMP_DIR"
          cp -a "$TMP_DIR"/. "$APP_DIR"/
          rm -rf "$TMP_DIR"
          rm -f "$PACKAGE_PATH"

          cat > "$APP_DIR/.env.production.local" << ENV_EOF
          NEXT_PUBLIC_SITE_URL=${{ secrets.NEXT_PUBLIC_SITE_URL }}
          NEXT_PUBLIC_GA_ID=${{ secrets.NEXT_PUBLIC_GA_ID }}
          NODE_ENV=production
          PORT=3000
          ENV_EOF

          cd "$APP_DIR"
          pm2 start "$(command -v bun)" --name yusuke-kim -- server.js
          pm2 save
          sleep 10
          curl -fsS http://127.0.0.1:3000/api/health
          REMOTE

      - name: Verify deployment
        run: |
          ssh deploy-server 'bash -s' << 'REMOTE'
          export BUN_INSTALL="$HOME/.bun"
          export PATH="$BUN_INSTALL/bin:$PATH"
          export NVM_DIR="$HOME/.nvm"
          [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"
          [ -n "${NVM_BIN:-}" ] && export PATH="$NVM_BIN:$PATH"
          pm2 status
          curl -fsS http://127.0.0.1:3000/api/health
          REMOTE
