// Shim: re-exports from @rebuildup/my-web-tools-ui (external/ui submodule).
// RawDOMContainer is a NAMED export (not default) — see Task 1 barrel audit.
export { RawDOMContainer, type BreadcrumbItem } from "../../../../external/ui/src/RawDOMContainer";
